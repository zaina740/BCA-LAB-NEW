#include<stdio.h>
#include<math.h>
main()
{ float a, area, para;
printf(" Enter the side of cube:");
scanf("%f",&a);
area=6*a*a;
para=12*a;
printf(" THE AREA IS :%.3f",area);
printf(" THE PARIMETR : %.3f",para);
}
